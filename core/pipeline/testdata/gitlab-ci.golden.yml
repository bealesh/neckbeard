# Generated by neckbeard — do not edit. Regeneration refuses modified files;
# change neckbeard.yaml or app-profile.yaml and re-run plan + scaffold.
# The test hook (.neckbeard/hooks/test.sh) is yours and never regenerated.

stages: [test, build, infra]

variables:
  AWS_REGION: us-east-1

.on_mr_or_default: &on_mr_or_default
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

.oidc: &oidc
  id_tokens:
    NECKBEARD_OIDC_TOKEN:
      aud: https://gitlab.com

test:
  stage: test
  image: ubuntu:24.04
  <<: *on_mr_or_default
  script:
    - ./.neckbeard/hooks/test.sh

policy:
  # The policy gate (DESIGN §10.1): checkov over every env root, honoring the
  # generated .checkov.yaml (skips are curated against this pinned version).
  stage: test
  image:
    name: bridgecrew/checkov:3.3.10
    entrypoint: [""]
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes: ["infra/**/*"]
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes: ["infra/**/*"]
  script:
    - checkov -d infra/envs/dev --quiet --compact --framework terraform --config-file .checkov.yaml
    - checkov -d infra/bootstrap/dev --quiet --compact --framework terraform --config-file .checkov.yaml
    - checkov -d infra/envs/stg --quiet --compact --framework terraform --config-file .checkov.yaml
    - checkov -d infra/bootstrap/stg --quiet --compact --framework terraform --config-file .checkov.yaml
    - checkov -d infra/envs/prd --quiet --compact --framework terraform --config-file .checkov.yaml
    - checkov -d infra/bootstrap/prd --quiet --compact --framework terraform --config-file .checkov.yaml

build:
  # Build once on the default branch; every environment runs this image by digest.
  stage: build
  image: docker:27
  services: ["docker:27-dind"]
  <<: *oidc
  environment: dev
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  variables:
    REGISTRY: $NECKBEARD_REGISTRY_DEV
  script:
    - docker build -f bellwether/Dockerfile -t "neckbeard-build:$CI_COMMIT_SHA" .
    # Scan gate: HIGH/CRITICAL block (DESIGN §10.1)
    - docker run --rm -v /var/run/docker.sock:/var/run/docker.sock aquasec/trivy image --severity HIGH,CRITICAL --exit-code 1 "neckbeard-build:$CI_COMMIT_SHA"
    - |
      if [ -z "$REGISTRY" ]; then
        echo "NECKBEARD_REGISTRY_DEV is not set (bootstrap pending, see docs/bootstrap.md) — build ran, push skipped"
        exit 0
      fi
      apk add --no-cache curl jq >/dev/null
      apk add --no-cache aws-cli >/dev/null
      echo "$NECKBEARD_OIDC_TOKEN" > /tmp/oidc-token
      export AWS_WEB_IDENTITY_TOKEN_FILE=/tmp/oidc-token AWS_ROLE_ARN="$NECKBEARD_AWS_APPLY_ROLE_DEV"
      aws ecr get-login-password --region "$AWS_REGION" | docker login --username AWS --password-stdin "${REGISTRY%%/*}"
      docker tag "neckbeard-build:$CI_COMMIT_SHA" "$REGISTRY:$CI_COMMIT_SHA"
      docker push "$REGISTRY:$CI_COMMIT_SHA"
      echo "Pushed digest:"
      docker inspect --format='{{index .RepoDigests 0}}' "$REGISTRY:$CI_COMMIT_SHA"

infra-dev:
  stage: infra
  image:
    name: ghcr.io/opentofu/opentofu:1.12.6 # pinned by the blueprint
    entrypoint: [""]
  <<: *oidc
  environment: dev
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes: ["infra/**/*"]
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes: ["infra/**/*"]
  script:
    - tofu -chdir=infra/envs/dev init -backend=false -input=false
    - tofu -chdir=infra/envs/dev validate
    - |
      if [ ! -f infra/envs/dev/backend.hcl ] || [ -z "$NECKBEARD_AWS_PLAN_ROLE_DEV" ]; then
        echo "bootstrap pending for dev (backend.hcl or CI variables missing, see docs/bootstrap.md) — validate-only run; plan/apply NOT EXERCISED"
        exit 0
      fi
      ROLE="$NECKBEARD_AWS_PLAN_ROLE_DEV"; if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then ROLE="$NECKBEARD_AWS_APPLY_ROLE_DEV"; fi
      echo "$NECKBEARD_OIDC_TOKEN" > /tmp/oidc-token
      export AWS_WEB_IDENTITY_TOKEN_FILE=/tmp/oidc-token AWS_ROLE_ARN="$ROLE"
      tofu -chdir=infra/envs/dev init -reconfigure -input=false -backend-config=backend.hcl
      tofu -chdir=infra/envs/dev plan -input=false -out=tfplan
      if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
        tofu -chdir=infra/envs/dev apply -input=false tfplan
      fi

infra-stg:
  stage: infra
  image:
    name: ghcr.io/opentofu/opentofu:1.12.6 # pinned by the blueprint
    entrypoint: [""]
  <<: *oidc
  environment: stg
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes: ["infra/**/*"]
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes: ["infra/**/*"]
  script:
    - tofu -chdir=infra/envs/stg init -backend=false -input=false
    - tofu -chdir=infra/envs/stg validate
    - |
      if [ ! -f infra/envs/stg/backend.hcl ] || [ -z "$NECKBEARD_AWS_PLAN_ROLE_STG" ]; then
        echo "bootstrap pending for stg (backend.hcl or CI variables missing, see docs/bootstrap.md) — validate-only run; plan/apply NOT EXERCISED"
        exit 0
      fi
      ROLE="$NECKBEARD_AWS_PLAN_ROLE_STG"; if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then ROLE="$NECKBEARD_AWS_APPLY_ROLE_STG"; fi
      echo "$NECKBEARD_OIDC_TOKEN" > /tmp/oidc-token
      export AWS_WEB_IDENTITY_TOKEN_FILE=/tmp/oidc-token AWS_ROLE_ARN="$ROLE"
      tofu -chdir=infra/envs/stg init -reconfigure -input=false -backend-config=backend.hcl
      tofu -chdir=infra/envs/stg plan -input=false -out=tfplan
      if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
        tofu -chdir=infra/envs/stg apply -input=false tfplan
      fi

infra-prd:
  stage: infra
  image:
    name: ghcr.io/opentofu/opentofu:1.12.6 # pinned by the blueprint
    entrypoint: [""]
  <<: *oidc
  environment: prd
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes: ["infra/**/*"]
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes: ["infra/**/*"]
      when: manual # prd gate: deployment approvals on Premium/Ultimate; protected branch otherwise (§11.3)
  script:
    - tofu -chdir=infra/envs/prd init -backend=false -input=false
    - tofu -chdir=infra/envs/prd validate
    - |
      if [ ! -f infra/envs/prd/backend.hcl ] || [ -z "$NECKBEARD_AWS_PLAN_ROLE_PRD" ]; then
        echo "bootstrap pending for prd (backend.hcl or CI variables missing, see docs/bootstrap.md) — validate-only run; plan/apply NOT EXERCISED"
        exit 0
      fi
      ROLE="$NECKBEARD_AWS_PLAN_ROLE_PRD"; if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then ROLE="$NECKBEARD_AWS_APPLY_ROLE_PRD"; fi
      echo "$NECKBEARD_OIDC_TOKEN" > /tmp/oidc-token
      export AWS_WEB_IDENTITY_TOKEN_FILE=/tmp/oidc-token AWS_ROLE_ARN="$ROLE"
      tofu -chdir=infra/envs/prd init -reconfigure -input=false -backend-config=backend.hcl
      tofu -chdir=infra/envs/prd plan -input=false -out=tfplan
      if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
        tofu -chdir=infra/envs/prd apply -input=false tfplan
      fi
