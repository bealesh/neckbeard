---
description: Re-run the neckbeard loop — plan, estimate, scaffold, validate — and walk me through what changed, what it costs, and what is NOT EXERCISED.
---

Load the neckbeard skill and run its plan → estimate → scaffold → validate loop
against the current `neckbeard.yaml` and `app-profile.yaml`:

- Show me the topology summary and the estimate's scenario table (plus the
  unpriced/unmodeled section) BEFORE scaffolding.
- Apply the ownership contract strictly: resolve scaffold conflicts with me
  rather than deleting my edits; never touch blueprint.yaml by hand.
- Finish with the validate report, spelling out exactly what PASSED, FAILED,
  and was NOT EXERCISED — and what the next human step is (usually
  docs/bootstrap.md).

$ARGUMENTS
